<?xml version="1.0" encoding="utf-8"?>
<!--
  A clickable RemoteViews body is used instead of Notification.contentIntent.
  SystemUI treats this like an inline notification control on many Android skins,
  so recording can toggle without the unconditional shade collapse associated
  with a normal notification-row content click.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/notification_body"
    android:layout_width="match_parent"
    android:layout_height="42dp"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/notification_title"
        style="@style/TextAppearance.Compat.Notification.Title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="14sp" />

    <Chronometer
        android:id="@+id/notification_chronometer"
        style="@style/TextAppearance.Compat.Notification.Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:gravity="center_vertical"
        android:maxLines="1"
        android:textSize="14sp"
        android:visibility="gone" />

</LinearLayout>
